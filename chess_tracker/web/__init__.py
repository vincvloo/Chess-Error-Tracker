"""Local web app: browse and manage a chess_tracker database from a browser
instead of the command line. Runs entirely on the user's own machine against
their own local SQLite file -- no hosted database, no server-side accounts."""
